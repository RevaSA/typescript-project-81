export const SINGLE_TAG : Record<string, string> = {
  BR: 'br',
  IMG: 'img',
  INPUT: 'input',
};

export const PAIRED_TAG: Record<string, string> = {
  FORM: 'form',
  LABEL: 'label',
  DIV: 'div',
};

export const SINGLE_TAGS = Object.values(SINGLE_TAG);

export const PAIRED_TAGS = Object.values(PAIRED_TAG);
